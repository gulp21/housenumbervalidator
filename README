Validates house numbers and finds dupes in OpenStreetMap files

Installation
============

Requirements: Qt4

qmake
make
./housenumbervalidator

Useful commands
===============
cat counter.txt | grep -P "hnrv\t" | awk -F "\t" '{print $1,"\t",$2,"\t",$3,"\t",$4}' | uniq > /tmp/counter
