Validates house numbers and finds dupes in OpenStreetMap files

Installation
============

Requirements: Qt4

qmake
make
./housenumbervalidator

Useful commands
===============
cat counter.txt | grep hnrv | sort -u > /tmp/counter
